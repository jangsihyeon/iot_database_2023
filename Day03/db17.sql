SELECT *
  FROM USERTBL;

SELECT B.NUM
	 , B.USERID
     , B.PRODNAME
     , U.NAME
     , U.ADDR
     , U.BIRTHYEAR
  FROM BUYTBL AS B
INNER JOIN USERTBL AS U
  ON B.USERID = U.USERID
ORDER BY B.NUM DESC;

